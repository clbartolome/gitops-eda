custom_credential_types:
  - name: "ServiceNow"
    kind: cloud
    inputs: "{{ lookup('file', 'casc/files/snow-inputs.json') }}"
    injectors: "{{ lookup('file', 'casc/files/snow-injectors.json') }}"
